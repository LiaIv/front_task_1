# AI Assistant Notes

В рамках ДЗ 8 проект был доработан с помощью AI-ассистента.

Что было сделано:
- перевод ключевых экранов и переиспользуемых компонентов на Material UI;
- подключение Storybook и добавление stories для переиспользуемых UI-компонентов;
- настройка Prettier, рекомендаций расширений VSCode и форматирование проекта;
- повторная проверка `npm run lint`, `npm run build` и `npm run build-storybook`.

Что было проанализировано после изменений:
- исправлен конфликт инициализации Storybook и JSX-конфига в `.storybook/preview`;
- исправлен lint-предупреждение в `vite.config.js` после Storybook init;
- подтверждено, что проект и Storybook собираются успешно.
